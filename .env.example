LLAMA_BIN_DIR=/home/user/work/llama.cpp/build-vulkan/bin
MODEL_DIR=/home/user/models
BACKEND_HOST=127.0.0.1
# Start port for per-model llama-server processes. Additional models use the
# next available ports, such as 8081, 8082, ...
BACKEND_PORT=8080
PROXY_HOST=0.0.0.0
PROXY_PORT=8000
DEFAULT_MAX_TOKENS=512
GRAMMAR_DEFAULT_MAX_TOKENS=256
MODEL_LOAD_TIMEOUT_SECONDS=60

# Optional. When set, API and management endpoints require:
# Authorization: Bearer <PROXY_API_KEY>
PROXY_API_KEY=
